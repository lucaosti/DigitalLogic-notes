//CX cifre in EBX
leggi_cifre:   
    CMP $0,%CX
    JE fine
    PUSH %CX
    PUSH %EBX
    PUSH %AX
cicla:
    CALL inchar
    CMP $'0', %AL
    JB cicla
    CMP $'9', %AL
    JA cicla
    CALL outchar
    AND $0x0F, %AL
    MOV %AL,(%EBX)
    INC %EBX
    DEC %ECX
    JNZ cicla
    POP %AX
    POP %EBX
    POP %CX
fine:
    RET
